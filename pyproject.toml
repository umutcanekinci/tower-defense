[project]
name = "chokepoint"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pygame-ce",
    "colorama",
    "pyyaml",
    "pytmx",
]

[tool.uv.sources]
pygame-core = { path = "src/pygame_core", editable = true }
